# ThePrompt

**Делись идеями. Создавай больше.**

Социальная платформа для обмена промптами к нейросетям — ChatGPT, Claude,
Gemini, Midjourney, DALL·E, Stable Diffusion и другим. Логика взаимодействия
построена по образцу X/Twitter: лента по центру, панели слева и справа, лайки,
треды, репосты, сохранённое и подписки — но всё заточено под публикацию
и обсуждение промптов.

Интерфейс — тёмный «AI community dashboard»: палитра `#151515 / #1A1A19 /
#111111`, акцент `#1683FF → #006DFF`, шрифт Inter, скругления 16–20px.

## Быстрый старт

```bash
npm install
cp .env.example .env     # необязательно: без него всё работает в демо-режиме
npm run seed             # демо-пользователи и промпты (необязательно)
npm start                # http://localhost:3000
```

Требуется **Node.js 22.5+** — используется встроенный модуль `node:sqlite`,
поэтому нативные зависимости компилировать не нужно.

### Как войти

Пароля нет: вход по одноразовому коду из письма.

* **Если SMTP не настроен** (по умолчанию) — код печатается в консоль сервера
  и подставляется в форму автоматически. Приложением можно пользоваться сразу.
* **Если настроен SMTP** — код уходит письмом, в ответе API его нет.

После `npm run seed` доступны аккаунты `admin@theprompt.dev` (администратор),
`nika@theprompt.dev`, `dev@theprompt.dev`, `art@theprompt.dev`.

### Кто становится администратором

* Если в `.env` заполнен `ADMIN_EMAILS` — все перечисленные адреса получают
  роль `admin` при входе.
* Если список пуст — админом становится первый зарегистрированный пользователь.

## Возможности

**Регистрация и вход**
* Регистрация по email, одноразовый код (OTP) на почту, без паролей.
* Ограничение частоты запросов, срок жизни кода 10 минут, лимит попыток ввода.
* Второй шаг — никнейм и отображаемое имя с проверкой занятости на лету.
* Вход через GitHub, Google, Microsoft и Discord (OAuth2, включается ключами
  в `.env`). Кнопки ChatGPT/Claude тоже есть — у OpenAI и Anthropic нет
  публичного OAuth «Войти через …» для сторонних сайтов, поэтому они честно
  объясняют это по клику вместо фиктивного входа.

**Лента**
* Вкладки «Свежее», «Популярное», «Подписки».
* Фильтры по модели ИИ, уровню сложности и категории; сортировка «свежее /
  популярное / обсуждаемое»; поиск; бесконечная прокрутка.

**Публикация промпта**
* Заголовок, текст промпта, модель ИИ и её точная версия (GPT-5.1,
  Midjourney v6.1…), уровень, категория, теги, описание и пример результата
  текстом или изображением.
* Кнопка копирования — прямо в блоке промпта на карточке.
* Опрос к сообществу: вопрос и до четырёх вариантов, один голос на человека,
  голос можно перенести на другой вариант.

**Взаимодействие**
* Лайки, комментарии с вложенными ответами (треды), репосты, сохранённое
  и жалобы с выбором причины.
* Подписки на авторов и отдельная лента «Подписки».

**Профиль**
* Широкая шапка-баннер, аватар заходит на её нижнюю границу — с чёрной
  обводкой, как в референсе. Имя, никнейм, био, счётчики, кнопка
  «Подписаться» / «Редактировать профиль».
* Аватар и баннер редактируются перед загрузкой в собственном редакторе
  обрезки: масштаб, перетаскивание, фиксированная рамка (круг для аватара,
  широкий прямоугольник 3:1 для баннера) — всё на canvas в браузере, без
  серверных библиотек обработки изображений.
* Вкладки «Промпты и репосты», «Понравилось» и приватная «Сохранённое».
* Счётчики публикаций, подписчиков, подписок, лайков и сохранённого.
* Золотой бейдж верификации Pro рядом с именем — на постах, в комментариях,
  в профиле и в блоке «Кого читать» (та же «печать», что у бейджа
  администратора, но другого цвета — как разные уровни галочек в других
  соцсетях).

**Уведомления и сообщения**
* «Уведомления» — отклики на ваши промпты: лайки, репосты, ответы, подписки.
* «Сообщения» — внутренняя переписка платформы: решения модерации, системные
  письма и уведомления о новых жалобах для администраторов.

**Админ-панель** (`/admin`, только для роли `admin`)
* Жалобы: кто пожаловался, на какой пост, причина, дата и комментарий.
* Действия: открыть пост, удалить и восстановить, предупредить автора,
  заблокировать и разблокировать, отклонить жалобу.
* Вкладки «Новые / Обработанные / Отклонённые», список пользователей, статистика.

**Оформление**
* Тёмная тема — основной режим; светлая доступна переключателем в правой панели
  и в настройках. Выбор хранится в аккаунте и в `localStorage`, применяется
  до первой отрисовки (без «вспышки»).
* Адаптив: три колонки на десктопе, свёрнутая панель на планшете, нижняя
  навигация и кнопка публикации на мобильном.
* `Ctrl/Cmd + K` — переход в поиск.

**Eduardo — ИИ-инструмент** (`/eduardo`)
* Три текстовых инструмента: вопрос-ответ и генерация тестов, генерация
  кода — с историей последних запросов. Вкладка «Изображение» пока
  показывает «скоро будет доступно» (генерация изображений ещё не
  подключена).
* Лимиты по месяцам: без подписки 5 текстовых запросов, с Pro — 20.
  Счётчик и прогресс-бар видны на странице; при исчерпании лимита форма
  честно отказывает и предлагает оформить Pro.
* Если задан `DEEPSEEK_API_KEY` — текстовые запросы уходят настоящей
  модели DeepSeek. Без ключа — прозрачный демо-режим: результат явно
  помечен «демо» и не выдаётся за настоящий ответ нейросети. Лимиты в
  демо-режиме считаются по-настоящему — это учебная имитация UX, а не
  подмена результата.
* Результат можно скопировать или сразу открыть в композере как черновик
  нового поста.

**ThePrompt Pro** (`/api/pro`, модалка из левой панели)
* Один тариф — $10/мес. Оплата не подключена (нет ключей платёжного
  провайдера): подписка активируется мгновенно по кнопке, честно помечена
  в интерфейсе как демо-режим. Даёт расширенные лимиты Eduardo и золотой
  бейдж верификации.

## Структура проекта

```
server/
  index.js         точка входа Express, статика, SPA-фолбэк, обработка ошибок
  config.js        конфигурация и чтение .env
  db.js            подключение SQLite, схема, миграции, помощники запросов
  store.js         запросы предметной области (лента, профили, опросы, уведомления)
  auth.js          сессии в куках и middleware доступа
  oauth.js         провайдеры OAuth2 (GitHub/Google/Microsoft/Discord)
  eduardo.js       движок Eduardo: вызов DeepSeek или демо-режим
  mailer.js        отправка писем (SMTP или вывод в консоль)
  constants.js     модели, уровни, категории, сортировки, причины жалоб, лимиты
  util.js          валидация, ошибки, ограничитель частоты
  routes/          auth, oauth, posts, users, uploads, notifications,
                   admin, pro, eduardo, testPrompt
  seed.js          демонстрационные данные
public/
  index.html       оболочка SPA
  css/styles.css   дизайн-система и адаптив
  js/
    app.js         маршруты и старт приложения
    api.js         обёртка над fetch
    state.js       глобальное состояние, тема, текущий пользователь
    router.js      мини-роутер на History API
    dom.js         DOM-помощники, модалки, тосты, буфер обмена
    icons.js       инлайновые SVG-иконки (+ картинки lobehub/Simple Icons
                   с автоматическим откатом), плитки моделей, логотип
    components/    shell, post, composer, auth, imageCropper, pro
    views/         home, explore, post, profile, settings, notifications,
                   messages, eduardo, admin, static
tests/
  api.test.js      сквозные тесты API (npm test)
  ui.mjs           браузерные сценарии на Playwright (запускаются отдельно)
```

## Тесты

```bash
npm test
```

46 сквозных проверок поверх реального HTTP-сервера и отдельной временной базы:
регистрация по OTP, вход через GitHub OAuth (со смоканным provider API, включая
повторный вход тем же аккаунтом), публикация и редактирование промптов,
заголовки и категории, лайки, треды комментариев, репосты, закладки, опросы
и голосование, подписки и лента подписок, фильтры, сортировка, поиск,
пагинация, жалобы, разделение уведомлений и сообщений, Pro-подписка и бейдж,
лимиты и демо-режим Eduardo (текст, тесты, код, изображения), все сценарии
админ-панели, защита от CSRF.

Отдельно лежит браузерный сценарий `tests/ui.mjs` (Playwright) — проверки
интерфейса: обе темы, копирование промпта, лента и фильтры, публикация,
лайки и комментарии, закладки, опрос с голосованием, жалоба, админ-панель,
«Сообщения», раздел Eduardo (все три инструмента и лимит), Pro-подписка,
редактор обрезки аватара и баннера, статические страницы, `Ctrl+K` и
мобильная вёрстка. Он не входит в `npm test`, потому что требует браузера:

```bash
npm i -D playwright && npx playwright install chromium
npm run seed && npm start        # в отдельном терминале
node tests/ui.mjs
```

## Переменные окружения

| Переменная | Назначение | По умолчанию |
| --- | --- | --- |
| `PORT` | порт HTTP-сервера | `3000` |
| `NODE_ENV` | режим работы | `development` |
| `SESSION_SECRET` | секрет сессий | случайный при старте |
| `SMTP_HOST` / `SMTP_PORT` / `SMTP_SECURE` | параметры SMTP | не заданы → демо-режим |
| `SMTP_USER` / `SMTP_PASS` | авторизация SMTP | пусто |
| `MAIL_FROM` | отправитель писем | `ThePrompt <no-reply@theprompt.local>` |
| `ADMIN_EMAILS` | админские адреса через запятую | пусто |
| `DB_FILE` | путь к файлу SQLite | `data/theprompt.db` |
| `APP_URL` | публичный адрес — для redirect_uri у OAuth | `http://localhost:${PORT}` |
| `GITHUB_CLIENT_ID` / `_SECRET` | OAuth-вход через GitHub | не заданы → кнопка неактивна |
| `GOOGLE_CLIENT_ID` / `_SECRET` | OAuth-вход через Google | не заданы → кнопка неактивна |
| `MICROSOFT_CLIENT_ID` / `_SECRET` | OAuth-вход через Microsoft | не заданы → кнопка неактивна |
| `DISCORD_CLIENT_ID` / `_SECRET` | OAuth-вход через Discord | не заданы → кнопка неактивна |
| `DEEPSEEK_API_KEY` / `DEEPSEEK_MODEL` | текст/тесты/код в Eduardo, `POST /api/test-prompt` | не задан → демо-режим / честная ошибка 503 |
| `OPENAI_API_KEY` / `OPENAI_IMAGE_MODEL` | задел под генерацию изображений | пока не используется — вкладка отключена |

## Безопасность

* Сессии — httpOnly-куки со случайным токеном, `SameSite=Lax`, срок 30 дней.
* Мутирующие запросы требуют заголовок `X-Requested-With` (защита от CSRF).
* Коды подтверждения хранятся только в виде SHA-256 хеша, сравнение —
  с постоянным временем; действуют ограничения по частоте и числу попыток.
* Пользовательский текст попадает в DOM только через `textContent`,
  разметка из данных не собирается.
* Загрузки — только изображения до 5 МБ, имена файлов генерирует сервер.
* Блокировка пользователя закрывает все его сессии и запрещает повторный вход.
* Сохранённое приватно: чужую вкладку «Сохранённое» API не отдаёт.

## Заметки по реализации

* Стек — Node.js + Express + SQLite и ванильный SPA на ES-модулях без сборщика.
  Дизайн-спецификация предполагала React/Next.js/Tailwind; проект остался
  на рабочем бэкенде без шага сборки, а визуальная часть спецификации выполнена
  полностью.
* Интерфейсные иконки — собственные инлайновые SVG. Логотипы нейросетей
  на плитках моделей и кнопках входа (GitHub/Google/Microsoft/Discord/
  ChatGPT/Claude) подгружаются готовыми картинками (lobehub, Simple Icons);
  если сеть недоступна, `<img onerror>` и таймаут откатывают их на
  абстрактную плитку — интерфейс не ломается.
* OAuth-вход — стандартный Authorization Code Flow, один модуль на всех
  реальных провайдеров (`server/oauth.js`). Первый вход через привязанный
  email заводит локальный аккаунт (или находит существующий с тем же
  адресом), повторный — узнаёт по `(provider, provider_user_id)`.
* Eduardo и Pro не требуют внешних ключей, чтобы попробовать продукт: без
  `DEEPSEEK_API_KEY` результат явно помечен `simulated`, без платёжного
  провайдера Pro просто активируется по кнопке — оба режима честно
  объявлены в интерфейсе, а не выданы за настоящие.
* Счётчики в правой панели показывают реальные числа из базы, а не витринные.
* SQLite-функция `lower()` работает только с ASCII, поэтому для поиска
  на кириллице зарегистрирована собственная функция `ulower()`.
* Элемент ленты — пост или репост (`type: 'post' | 'repost'`); лента подписок
  собирается через `UNION ALL`.
* Удаление постов и комментариев мягкое (`deleted_at`): модератор может
  восстановить публикацию, а история жалоб остаётся целой.
* Схема обновляется догоняющими миграциями (`ALTER TABLE ... ADD COLUMN`),
  поэтому база из предыдущей версии продолжает работать.
